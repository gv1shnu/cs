# Personal notes 🔒

Everything committed here is generated output — ciphertext.

## Usage (me)

Ship it (build → commit → push):

```
STATICRYPT_PASSWORD='…' ./publish.sh
```

Get the source back from what's committed:

```
STATICRYPT_PASSWORD='…' ./decrypt.sh
```
